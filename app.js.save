const fs = require("fs");
const readline = require("readline");

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});
//console.log("arguments",process.argv);

/*
fs.readdir("./", function(err, data) {
  for(const dirent of data) {
    console.log(dirent);
  }
});
*/

/*
try {
  const bundle = fs.readFileSync('./app/assets/javascripts/easy_vue/bundle.js', 'utf8');
  console.log("file found");
} catch(err) {
  console.log("Bundle not found");
}
*/


let branch = "";
let commit = "";

const getBranch = (name) => {
  branch = name;
  rl.on("close", function() {
    console.log("\nBYE BYE !!!");
    process.exit(0);
});
}

const getCommit = (name) => {
 commit = name;
}

rl.question("Branch name: ", getBranch);
